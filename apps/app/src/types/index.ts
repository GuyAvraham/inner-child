export enum Age {
  Young = 'young',
  Old = 'old',
}

export enum Onboarded {
  Current = 'current',
  Generate = 'generate',
  Finished = 'finished',
}

export enum Role {
  User = 'user',
  Assistant = 'assistant',
  System = 'system',
}

export enum ConversationStatus {
  Idle = 'idle',
  Waiting = 'waiting',
}
